[package]
name = "quake3-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
quake3 = { path = "../quake3" }

[badges]
maintenance = { status = "experimental" }
